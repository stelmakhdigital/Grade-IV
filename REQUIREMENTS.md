# REQUIREMENTS.md — Спецификация требований (SRS) сервиса «Грейд»

> **Версия: v1.0-черновик (2026-09-09, фаза Requirements).** Основано на результатах Discovery
> (решения #1–16 в `PROJECT_MEMORY.md`). Phase gate — после одобрения пользователем.

## 1. Назначение и рамки MVP
Веб-сервис ИИ-мок-интервью для разработчиков. Кандидат проходит полноценное интервью:
живой голосовой диалог с ИИ-интервьюером (только русский язык), стадия Live-Code, стадия
System Design (Middle/Senior/Staff) и итоговый отчёт. 60 минут бесплатно, далее — поминутный
учёт (платёжный шлюз в MVP не реализовывается).

**В скоупе MVP:**
- Голосовое интервью: STT → LLM (Qwen3.8-27B) → TTS, ходовой диалог («живой» стиль: интонации,
  заполнение пауз). Self-hosted: faster-whisper large-v3-russian (GPU), Silero v5 (MIT);
  эндпоинты `/api/v1/stt`, `/api/v1/tts` + абстракция провайдера.
- Live-Code: редактор (Monaco), задачи Go/Python, сандбокс (Docker) с тестами, ИИ-ревьюер.
- System Design (Middle+): whiteboard — палитра архитектурных блоков + свободное рисование;
  ИИ-оценка схемы (vision) + устного ответа.
- Итоговый отчёт: оценки по критериям, сильные/слабые стороны, рекомендации.
- Кабинет кандидата: профиль, история сессий, баланс минут.
- Поминутная тарификация (60 мин бесплатно, лимиты) без платёжного шлюза.
- Грейды: Junior / Middle / Senior / Staff; стеки MVP: **Go, Python**.

## 2. Акторы
| Актор | Описание |
|---|---|
| Кандидат | Разработчик, проходит мок-интервью (голос + код + схема) |
| ИИ-интервьюер | Системная роль: ведёт сессию по программе грейда/стека, оценивает на лету |
| Администратор | Вне MVP: только логи, метрики и БД (NFR-9) |

## 3. Ключевой сценарий (happy path)
1. Кандидат регистрируется (email) → кабинет: баланс минут, список сессий, «Начать».
2. Выбор грейда (время: Junior 45, Middle 50, Senior 60, Staff 75 мин) и стека (Go/Python) → старт.
3. **Голосовое интервью**: ИИ открывает сессию, ведёт диалог по программе грейда
   (CS, deep-dive по стеку, поведенческие; блоки программы — как в прототипе лендинга),
   задаёт follow-up вопросы, заполняет долгие паузы.
4. **Live-Code**: ИИ объявляет задачу (банк задач по стеку/грейду), кандидат пишет код в
   Monaco, запускает тесты в сандбоксе, получает результат; после сдачи — ИИ-ревью кода
   и follow-up вопросы (голосом и текстом).
5. **System Design** (Middle+): задача на whiteboard (палитра блоков + рисование),
   кандидат объясняет решение голосом, ИИ задаёт 2–4 уточняющих вопроса, оценивает
   схему (по изображению/структуре) и ответ.
6. ИИ завершает сессию → **отчёт**: оценки по критериям, сильные/слабые стороны,
   рекомендации по подготовке.
7. Кабинет: отчёт, транскрипт, код, схема — в истории сессии.

## 4. User stories и критерии приёмки (MoSCoW)

| ID | История | Приоритет | Критерии приёмки |
|---|---|---|---|
| US-1 | Как кандидат, хочу зарегистрироваться и видеть кабинет, чтобы начать интервью | **Must** | Регистрация/вход по email + паролю; кабинет: баланс минут, список сессий (дата, грейд, стек, оценка, статус), кнопка «Начать»; данные переживают перезагрузку |
| US-2 | Как кандидат, хочу выбрать грейд и стек, чтобы сессия соответствовала моему уровню | **Must** | Выбор грейда (4) и стека (Go/Python); время сессии по грейду; в программу Middle+ включена стадия System Design, Junior — нет |
| US-3 | Как кандидат, хочу говорить с ИИ-интервьюером, как на живом интервью | **Must** | Голос через микрофон; ИИ отвечает голосом на русском (интонации, паузы); понимает ответы и задаёт follow-up; пауза кандидата > 8 с → ИИ переспрашивает/подталкивает; latency: конец реплики → начало ответа p50 < 2,5 с, p95 < 4 с |
| US-4 | Как кандидат, хочу решать кодинг-задачу и проверять решение в процессе | **Must** | Monaco-редактор (Go/Python) с условием задачи; «Запустить тесты» → результат в UI ≤ 10 с; после сдачи — ИИ-ревью (комментарии к коду) и follow-up вопросы |
| US-5 | Как кандидат (Middle+), хочу спроектировать систему и получить оценку решения | **Must** | Whiteboard: палитра блоков (см. FR-S5) + стрелки + свободное рисование + заметки; голосовое объяснение решения; 2–4 вопроса от ИИ; в отчёте — оценка схемы и ответа отдельно |
| US-6 | Как кандидат, хочу получить отчёт, чтобы понимать свой уровень | **Must** | Отчёт генерируется ≤ 2 мин после конца сессии: оценки по критериям (CS, стек, Live-Code, System Design, коммуникация), сильные/слабые стороны, рекомендации; доступен в истории |
| US-7 | Как кандидат, хочу видеть остаток времени, чтобы управлять сессией | **Must** | Таймер в UI; предупреждение при остатке ≤ 5 мин; при 0 — корректное завершение и отчёт; расход минут — в истории |
| US-8 | Как кандидат, хочу не потерять сессию при обрыве связи | Should | Обрыв/пауза → автостоп таймера; возобновление тем же этапом в течение 24 ч; > 30 мин без возобновления — сессия финализируется с отчётом |
| US-9 | Как кандидат, хочу скачать/поделиться отчётом | Could | Экспорт PDF и/или публичная ссылка на отчёт |
| US-10 | Как кандидат, хочу повторить интервью, чтобы отслеживать прогресс | Should | Кнопка «Повторить» (тот же грейд/стек); в истории — динамика оценок по критериям |

## 5. Функциональные требования

### 5.1. Аутентификация и кабинет (FR-A)
- **FR-A1.** Регистрация и вход по email + паролю; сессия — JWT.
- **FR-A2.** Кабинет: баланс минут, список сессий (дата, грейд, стек, оценка, статус), «Начать новое интервью».
- **FR-A3.** Форма старта: грейд (Junior/Middle/Senior/Staff) + стек (Go/Python); длительность — по грейду (45/50/60/75 мин).
- **FR-A4.** История: детальная страница сессии — транскрипт голосовой части, код и результаты тестов (Live-Code), схема (System Design), отчёт.

### 5.2. Сессия и стадии (FR-S)
- **FR-S1.** Машина состояний сессии: `started → voice → livecode → design (Middle+) → report → finished | paused | aborted`.
- **FR-S2.** Голосовая стадия: ИИ ведёт сессию по программе грейда/стека (блоки программы берутся из прототипа лендинга); 1–2 follow-up на тему; скрытая оценка ответа в процессе.
- **FR-S3.** Поведение ИИ: заполнение паузы > 8 с; корректная реакция на «не знаю»; короткие подтверждения понимания; вопросительная/восклицательная интонация — через TTS (Silero v5).
- **FR-S4.** Live-Code: задача из банка (по стеку и грейду; минимум банка — в Design); Monaco (Go/Python); запуск тестов в сандбоксе (FR-C); после «сдачи» — ИИ-ревью кода (комментарии) + follow-up вопросы голосом и/или текстом.
- **FR-S5.** System Design (Middle+): whiteboard — палитра блоков (Client, Load Balancer, API-сервис, Worker, SQL-БД, NoSQL, Кэш, Очередь, Message Broker, CDN, Внешний API, Monitoring), соединения-стрелки, свободное рисование (Excalidraw-стиль), текстовые заметки; ИИ оценивает структуру (vision-анализ схемы + семантика блоков/связей) и устный ответ; 2–4 follow-up.
- **FR-S6.** Управление: завершение по инициативе ИИ (время вышло/стадии пройдены) или кандидата («завершить»).
- **FR-S7.** (Should) Возобновление после обрыва: сессия ставится в `paused`, возобновляется с того же этапа (US-8).

### 5.3. Голосовой контур (FR-V)
- **FR-V1.** Контур: микрофон браузера → (WebSocket) → STT-сервис → LLM-сервер (Qwen3.8-27B, vLLM) → TTS-сервис → аудио в браузер.
- **FR-V2.** STT: faster-whisper large-v3-russian на GPU, чанки/потоковая обработка; API `POST /api/v1/stt` (аудио-чанк → текст/гипотезы).
- **FR-V3.** TTS: Silero v5 (MIT), стриминг по фразам; API `POST /api/v1/tts` (текст → аудио-поток).
- **FR-V4.** Latency (ходовой режим): конец реплики кандидата → начало аудио ИИ — p50 < 2,5 с, p95 < 4 с.
- **FR-V5.** VAD: окончание реплики кандидата — тишина 0,7–1,2 с (настраивается).
- **FR-V6.** Заполнение паузы: тишина кандидата > 8 с → ИИ задаёт подталкивающий вопрос.
- **FR-V7.** Абстракция провайдера STT/TTS (интерфейсы + `/api/v1/*`) — провайдеры подменяемы без изменения конвейера.
- **FR-V8.** (Should) Деградация: при недоступности STT/TTS сессия продолжается в текстовом режиме (кандидат пишет, ИИ отвечает текстом) с уведомлением.

### 5.4. Сандбокс (FR-C)
- **FR-C1.** Выполнение кода Go/Python с тестами: Docker-контейнер на сессию, лимиты: CPU ≤ 1 ядро, RAM ≤ 512 МБ, время ≤ 10 с, сеть запрещена, непривилегированный пользователь.
- **FR-C2.** Результаты тестов (pass/fail, stdout/stderr) — в UI в течение ≤ 10 с.
- **FR-C3.** Код и результаты сохраняются в сессии (FR-A4).

### 5.5. Отчёт (FR-R)
- **FR-R1.** Отчёт: оценки по критериям (CS, стек, Live-Code, System Design, коммуникация — шкала 1–5), рекомендуемый грейд, сильные/слабые стороны, рекомендации.
- **FR-R2.** Генерация ≤ 2 мин после завершения; хранение и просмотр в кабинете.
- **FR-R3.** (Could) Экспорт PDF / публичная ссылка (US-9).

### 5.6. Тарификация (FR-B)
- **FR-B1.** Новый аккаунт — 60 минут бесплатно.
- **FR-B2.** Поминутный учёт по определению раздела 7; отображение остатка в кабинете и во время сессии.
- **FR-B3.** Остаток 0 → новый старт заблокирован (MVP: подсказка «минуты закончились», без платёжного шлюза).
- **FR-B4.** Пауза (кандидат/система) — таймер не идёт.

## 6. Нефункциональные требования (NFR)
| ID | Требование |
|---|---|
| NFR-1 | Latency голосового контура — FR-V4 (p50/p95); метрики по каждому этапу (FR/Design: ADR по замерах) |
| NFR-2 | Одновременно: ≥ 5 активных сессий на GPU-узел (MVP); планирование ёмкости в Design |
| NFR-3 | Доступность ≥ 99% (рабочие часы); деградация — FR-V8 (текстовый режим) |
| NFR-4 | Безопасность: изоляция сандбокса (FR-C1); JWT-аутентификация; без PII в логах; секреты — вне репозитория |
| NFR-5 | Хранение данных сессий (транскрипт, код, схема, отчёт) ≥ 12 месяцев |
| NFR-6 | Отчёт — ≤ 2 мин; загрузка интерфейса — ≤ 3 с |
| NFR-7 | Локализация: только русский (UI, интервью, голос ИИ) |
| NFR-8 | Браузеры: Chrome/Edge/Firefox (два последних релиза), доступ к микрофону |
| NFR-9 | Наблюдаемость: логи + метрики (latency конвейера, ошибки STT/TTS, загрузка GPU, сессии) + алерты |
| NFR-10 | Лицензии: все self-hosted компоненты — MIT/Apache/BSD (faster-whisper MIT, Silero v5 MIT, Qwen3.8-27B Apache 2.0, Excalidraw MIT) |

## 7. Определение «минуты интервью» (решение R8)
**Тарифицируется активное время сессии** — от нажатия «Начать» до завершения (отчёт сгенерирован):
- Все стадии считаются одинаково (голос, Live-Code, System Design).
- **Не тарифицируется**: время в кабинете до старта; паузы (по инициативе кандидата/системы);
  простои сервиса; обрыв связи (таймер на паузе).
- Обрыв без возобновления > 30 мин → сессия финализируется; тарифицируется фактически
  прошедшее активное время.
- Длительность по грейду (45/50/60/75) — верхний лимит; при достижении лимита сессия
  завершается корректно.
- Числовые пороги (0,7–1,2 с VAD, 8 с пауза, 30 мин таймаут) — конфигурируемые.

## 8. Вне скоупа MVP (явно)
- Платёжный шлюз, подписки, B2B-сценарии.
- Стеки вне Go/Python (JS/TS, Java, C++, Rust, PHP, Kotlin) — архитектура допускает добавление.
- Английский язык интервью (и голос).
- Full-duplex с barge-in (кандидат прерывает ИИ) — MVP ходовой режим; barge-in — апгрейд.
- Живой (человеческий) разбор сессий.
- Мобильные приложения (MVP — responsive Web).
- Админ-панель (MVP — метрики/логи/БД).

## 9. Открытые вопросы (закрываются в Design)
- Тарифные цифры за минуту (после MVP).
- Минимальный размер банка задач Live-Code (предложение: ≥ 20 задач на стек × грейд).
- Итоговая палитра блоков whiteboard и правила валидации схемы.
- Критерии отчёта по грейдам (какие критерии/веса для Junior…Staff) — SRS v1.1.

## 10. Глоссарий
- **STT/ASR** — распознавание речи; **TTS** — синтез речи; **VAD** — детектор активности голоса.
- **Barge-in** — прерывание ответа ИИ голосом кандидата (вне MVP).
- **Сандбокс** — изолированный Docker-контейнер выполнения пользовательского кода.
- **Whiteboard** — холст System Design (блоки + рисование).
- **Live-Code** — стадия живого кодинга с ИИ-ревьюером.

## 11. Одобрение (phase gate)
- [ ] Требования рассмотрены и одобрены пользователем (дата: ____).
- После одобрения: коммит, переход в фазу Design (WBS, ADR, архитектура v0.3).
